---
const values = [
  [
    {
      ref: '01',
      name: 'Performance Obsessed',
      description:
        'We believe speed is the foundation of digital success. From sub-second load times to top Core Web Vitals, every project we deliver is engineered for maximum performance. A faster site isn’t just a technical win — it means happier users, higher rankings, and stronger conversion rates.',
    },
    {
      ref: '02',
      name: 'Modular by Design',
      description:
        'One size doesn’t fit all, and growth is never linear. Our modular approach lets you start with the essentials — a fast, SEO-strong foundation — and add layers as you need them. Whether it’s CRM integrations, AI optimisation, or advanced automation, every piece connects seamlessly into the bigger system.',
    },
  ],
  [
    {
      ref: '03',
      name: 'Long-Term Partners',
      description:
        'We don’t believe in “launch and leave.” Technology evolves too fast for that. Instead, we build long-term partnerships, continuously monitoring, optimising, and evolving your platform. This way, your website never falls behind, and your business keeps compounding results.',
    },
    {
      ref: '04',
      name: 'Transparent Growth',
      description:
        'Our approach is grounded in clarity and accountability. We focus on measurable outcomes, real data, and strategies that create lasting value. No hidden complexity, no vanity metrics — just digital platforms that prove their worth through results you can see and track.',
    },
  ],
];
---

<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="flex flex-col gap-8 lg:gap-12">
      <h2 class="text-3xl font-medium tracking-tight sm:text-4xl">Our Values</h2>
      <div class="grid gap-8 lg:grid-cols-2">
        {
          values.map((column, index) => (
            <div class:list={['space-y-8', index === 1 && 'lg:mt-16']}>
              {column.map((value) => (
                <div class="flex flex-col gap-12 rounded-3xl bg-primary-500/10 px-6 py-8 dark:bg-primary-400/10">
                  <p class="text-sm">{value.ref}</p>
                  <div class="flex flex-col gap-4">
                    <h3 class="text-xl font-medium">{value.name}</h3>
                    <p class="text-base text-primary-950/70 dark:text-primary-200/70 sm:text-lg">
                      {value.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
